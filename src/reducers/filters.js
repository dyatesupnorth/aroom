const filtersReducerDefaultState = {
  text: "",
  starRating: "",
  facilities: []
};
export default (state = filtersReducerDefaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
